<!-- $TenDRA$ -->

<!ENTITY alpha-common-desc '
  <para><command>alphatrans</command> compiles a target dependent TDF capsule
    into an AXP assembler file.</para>

  <para>It is recommended that <command>alphatrans</command> not be invoked
    directly, but via the user interface &man.tendra.tcc;. Alpha specific options
    may be passed to <command>alphatrans</command> by passing the option
    <option>-Wt,-</option><replaceable>opt</replaceable> to
    &man.tendra.tcc;.</para>
'>


<!--   K   -->
<!ENTITY common-alpha-option-ck '
    <varlistentry>
      <term><option>-K</option><replaceable>str</replaceable></term>

      <listitem>
        <para>Causes <command>alphatrans</command> to optimise its code
          for a particular processor model, determined by
          <emphasis>str</emphasis>. Currently, this option has no
          effect.</para>
      </listitem>
    </varlistentry>
'>
