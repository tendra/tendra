# $Id$

.include "Makefile.inc"


STARTUPSUBDIR+=	bounds.32
STARTUPSUBDIR+=	bounds.ansi
STARTUPSUBDIR+=	literal
STARTUPSUBDIR+=	modes
STARTUPSUBDIR+=	modes.cpp
STARTUPSUBDIR+=	promotion
STARTUPSUBDIR+=	charmap

.include <tendra.startup.mk>

