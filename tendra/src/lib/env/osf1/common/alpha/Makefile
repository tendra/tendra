# $TenDRA$

.PATH : ${.CURDIR} ${.CURDIR}/../../../common

DATA=	posix_lib static tcc_diag tcc_prof

INSTALL_ENV=	yes

# This lives in env/, and sucks in the env/common files.
.include "../../../Makefile.inc"

.include "../../../../../../mk/base/tendra.prog.mk"
