# $TenDRA$

.PATH : ${.OBJDIR}/../../../posix2.api

TCCFLAGS+=	-g

# Source defines for the api itself
.include "${.CURDIR}/../Makefile.inc"

# Flags for tspec
.include "${.CURDIR}/../../Makefile.inc"
