# $Id$

.include "Makefile.inc"


.if !defined(NOTESTS)
SUBDIR+=	tdf
.endif

.if !defined(NOTESTS)
SUBDIR+=	c89-hosted
SUBDIR+=	c89-common
SUBDIR+=	c89-library
.endif

.if !defined(NOTESTS)
SUBDIR+=	posix.1-2008
.endif

.include <tendra.subdir.mk>

